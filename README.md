
```
conda create -n proton-realy-wires python=3.12
conda activate proton-realy-wires
pip install -r requirements.txt
```

```
it might be plausible to use conda instead as it can handle java for caver
```
